export const containerMaxW = 'xl:max-w-6xl xl:mx-auto'

export const appTitle = 'React Ticketing - Yudha Eko Praditya'

export const getPageTitle = (currentPageTitle: string) => `${currentPageTitle} — ${appTitle}`
